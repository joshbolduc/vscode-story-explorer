import { Meta } from '@storybook/react';

export const TypeScriptStory = () => 'Story for TS with typed identifier';

const meta: Meta = {
  title: 'Test/TypeScript/Typed Identifier',
};

export default meta;
