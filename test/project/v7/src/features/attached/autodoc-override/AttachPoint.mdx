import { Meta } from '@storybook/blocks';
import * as ComponentStories from './AttachPoint.stories';

<Meta of={ComponentStories} />

This is an attached MDX doc named `Autodoc.mdx`, which conflicts with the Autodoc title
