import { Meta } from '@storybook/blocks';
import * as ComponentStories from './AttachPoint.stories.js';

<Meta of={ComponentStories} name="Attached to path with extension" />

This is an attached MDX doc attached to a relative path with a file extension
