import { Meta } from '@storybook/blocks';
import * as ComponentStories from './AttachPoint.stories';

<Meta of={ComponentStories} name="Manually specified name" />

This is an attached MDX doc with a specified name
