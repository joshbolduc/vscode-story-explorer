import { Meta } from '@storybook/blocks';
import * as ComponentStories from './../attached/././AttachPoint.stories';

<Meta of={ComponentStories} name="Attached to non-normalized path" />

This is an attached MDX doc using a path that needs to be normalized
