import { Meta } from '@storybook/blocks';
import * as ComponentStories from './AttachPoint.stories';

<Meta of={ComponentStories} />

This is an attached MDX doc named `Sample.mdx`, which matches the component name (`Sample`)
