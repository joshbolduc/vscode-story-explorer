import { Meta } from '@storybook/blocks';
import * as ComponentStories from './AttachPoint.stories';

<Meta of={ComponentStories} name="Name/with/slashes" />

This is an attached MDX doc with a specified name with slashes, which should not be interpreted as part of the hierarchy
