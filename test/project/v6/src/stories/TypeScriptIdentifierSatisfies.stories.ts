import { Meta } from '@storybook/react';

export const TypeScriptStory = () =>
  'Story for TS with identifier using satisfies, exported';

const meta = {
  title: 'Test/TypeScript/Identifier Satisfies',
} satisfies Meta;

export default meta;
