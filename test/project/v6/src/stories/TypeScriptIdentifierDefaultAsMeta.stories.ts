import { Meta } from '@storybook/react';

export const TypeScriptStory = () =>
  'Story for TS with exported identifier as Meta';

const meta = {
  title: 'Test/TypeScript/Identifier Default Exported as Meta',
};

export default meta as Meta;
