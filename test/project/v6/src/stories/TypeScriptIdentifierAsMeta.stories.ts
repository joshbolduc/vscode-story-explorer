import { Meta } from '@storybook/react';

export const TypeScriptStory = () =>
  'Story for TS with identifier as Meta, exported';

const meta = {
  title: 'Test/TypeScript/Identifier as Meta',
} as Meta;

export default meta;
